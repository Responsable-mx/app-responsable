"use client";

// Mockup completo de la app: Chat, Clientes, Configuración.
// Sin backend, sin Supabase, sin auth.
// Acceso: http://localhost:3000/dev/app-preview

import { AppShell } from "./AppShell";

export default function AppPreview() {
  return <AppShell />;
}
